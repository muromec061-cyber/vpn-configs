{
    "name": "Chisel VPN Tunnel",
    "image": "ubuntu:22.04",
    "postCreateCommand": "apt-get update -qq && apt-get install -y -qq curl ca-certificates && curl -sL -o /tmp/chisel.gz 'https://github.com/jpillora/chisel/releases/download/v1.11.8/chisel_1.11.8_linux_amd64.gz' && gunzip -f /tmp/chisel.gz && chmod +x /tmp/chisel && cp /tmp/chisel /usr/local/bin/chisel",
    "postStartCommand": "nohup chisel server --port 8080 --reverse --keepalive 25s > /tmp/chisel.log 2>&1 &",
    "forwardPorts": [8080],
    "portsAttributes": {
        "8080": {
            "label": "Chisel Server",
            "onAutoForward": "notify",
            "requireLocalPort": false
        }
    }
}
